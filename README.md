# Auralis

Motor cognitivo mínimo en Rust (AuriaLABS).
Transformer decoder-only, backprop y Adam a mano.

```bash
cargo test
cargo run --release -- train 80 auralis.bin
cargo run --release -- eval auralis.bin
```
